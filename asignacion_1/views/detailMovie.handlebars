<div class="col-md-12">
    <div class="row">
        <div class="col-md-3 form-group">
            <div class="row">
                <h1>{{name}}</h1>
            </div>

            <div class="row">
                {{> keywords}}
            </div>
            
            {{#if imageCompressed}}
                <img src="{{imageCompressed}}" class="Image">
            {{else}}
                <img src="{{image}}" class="Image">
            {{/if}}
        </div>
    </div>
    <br>
    <div class="row">
        <span class="white-space">{{description}}</span>
    </div>
</div>